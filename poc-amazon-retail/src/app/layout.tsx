export const metadata = {
  title: "Amazon Poc",
  description: "amazon retail poc",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
