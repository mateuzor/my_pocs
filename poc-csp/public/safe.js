// Loaded from /safe.js — same origin as the page, so CSP allows it under script-src 'self'.
const el = document.getElementById('safe-result');
if (el) el.textContent = '✅ safe.js loaded (same-origin script — allowed by script-src \'self\')';
