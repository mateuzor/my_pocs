# Any NITRO_* variable overrides the matching runtimeConfig key at runtime.
# Copy to .env (gitignored) and adjust. Nested keys use underscores:
#   NITRO_API_SECRET       -> runtimeConfig.apiSecret
#   NITRO_PUBLIC_APP_NAME  -> runtimeConfig.public.appName
NITRO_API_SECRET=super-secret-in-prod
