# Copie para .env e ajuste os valores
PORT=3000
NODE_ENV=development
LOG_LEVEL=debug
DATABASE_URL=./data/tasks.db

# Aula 8 — troque por um valor aleatório longo em produção
# (ex.: openssl rand -base64 32)
JWT_SECRET=dev-only-secret-nao-use-em-prod
JWT_EXPIRES_IN=15m
