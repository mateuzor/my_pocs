{
  "name": "poc-build-benchmark",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bench:webpack": "node scripts/run.js webpack",
    "bench:vite": "node scripts/run.js vite",
    "bench:esbuild": "node scripts/run.js esbuild",
    "bench": "npm run bench:webpack && npm run bench:vite && npm run bench:esbuild"
  },
  "devDependencies": {
    "esbuild": "^0.22.0",
    "vite": "^5.2.0",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
