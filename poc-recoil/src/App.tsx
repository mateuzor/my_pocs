// Day 1 skeleton: just proves RecoilRoot is wired. The atoms, derived
// selectors and async selectors land in the following commits.
export function App() {
  return (
    <main style={{ fontFamily: 'system-ui', maxWidth: 640, margin: '2rem auto' }}>
      <h1>Recoil POC</h1>
      <p>Atom graph + derived selectors + async selectors.</p>
    </main>
  );
}
